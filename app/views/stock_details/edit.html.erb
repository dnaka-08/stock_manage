<div class="row justify-content-center">
  <div class="col-sm-6 offset-sm-2">
    <%= form_with(model: @stock_detail, local: true) do |f| %>
      <%= render 'layouts/error_messages', model: f.object %>
    
      <div class="form-group">
        <%= f.label :store_id, '対象店舗：' %>
        <%= f.collection_select :store_id, Store.all, :id, :name, :include_blank => true %>
      </div>
      <div class="form-group">
        <%= f.label :product_id, '対象商品：' %>
        <%= f.collection_select :product_id, Product.all, :id, :name, :include_blank => true %>
      </div>
      <div class="form-group">
        <%= f.label :date, '日付：' %>
        <%= f.date_select :date, class: 'form-control', :use_month_numbers => true %>
      </div>

      <div class="form-group">
        <div class="form-inline">
          <%= f.label :number, '入庫数：' %>
          <%= f.text_field :in_number, class: 'form-control' %>
        </div>
      </div>

      <div class="form-group">
        <div class="form-inline">
          <%= f.label :number, '出庫数：' %>
          <%= f.text_field :out_number, class: 'form-control' %>
        </div>
      </div>

      <div class="form-group">
        <div class="form-inline">
          <%= f.label :number, '破棄数：' %>
          <%= f.text_field :drop_number, class: 'form-control' %>
        </div>
      </div>
      
      <%= f.hidden_field :user_id, :value => session[:user_id] %>

      <%= f.submit '新規登録', class: 'btn btn-primary btn-block col-sm-6' %>
    <% end %>
  </div>
</div>